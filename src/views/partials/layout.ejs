<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    /* Estilo para el sticky footer */
    html, body {
      height: 100%;
    }
    .container-fluid {
      display: flex;
      flex-direction: column;
      min-height: 100%;
    }
    main {
      flex: 1; /* Hace que el contenido principal ocupe el espacio restante */
    }
    nav.sidebar {
      height: 100vh; /* Hace que el menú lateral ocupe toda la altura de la ventana */
      position: sticky;
      top: 0;
    }
    footer{
      margin-top: auto;
    }
  </style>
</head>
<body>
  <div class="container-fluid">
    <div class="row">
      <!-- Menú lateral -->
      <%- include('menu') %>

      <!-- Contenido principal -->
      <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 py-4" id="main-content">
        
        <%- body %>
      </main>
    </div>
  </div>
  <%- include('footer') %>
</body>
</html>

